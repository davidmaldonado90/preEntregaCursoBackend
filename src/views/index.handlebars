<main>
    {{#each products}}
        <div class=" product">
            <h1>Nombre del producto: {{this.tittle}}</h1>
            <p>Descripcion: {{this.description}}</p>
            <p>Categoria: {{this.category}}</p>
        </div>
    {{/each}}
</main>

